---
trigger:
  branches:
    include:
    - 'main'
  paths:
    include:
    - environments/dev

pr: none

pool: 'Case Mgmt PreProd Pool'

jobs:
  - template: templates/terragrunt_apply.yml
    parameters:
      environment: dev
      module: applications
      subscription: "Azure Pipeline: Case Management - Pre-Prod" #service principal
      backend_storage_resource_group: "rg-casemgmt-devops-preprod"
      backend_storage_account_name: "sacasemgmttfstatedev"
      application_name: "applications"

  - template: templates/terragrunt_apply.yml
    parameters:
      environment: dev
      module: base
      subscription: "Azure Pipeline: Case Management - Pre-Prod" #service principal
      backend_storage_resource_group: "rg-casemgmt-devops-preprod"
      backend_storage_account_name: "sacasemgmttfstatedev"
      application_name: "base"
